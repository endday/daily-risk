{
  "name": "daily-risk",
  "private": true,
  "workspaces": [
    "frontend",
    "worker"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev:remote",
    "dev:frontend": "cd frontend && npm run dev:remote",
    "dev:remote": "cd frontend && npm run dev:remote",
    "dev:worker": "cd worker && npm run dev",
    "build": "npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "build:worker": "cd worker && npm run build",
    "deploy": "npm run deploy:worker",
    "deploy:worker": "cd worker && npx wrangler deploy"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "puppeteer": "^24.43.1"
  }
}
